services:
  polizas-api:
    build: .
    container_name: polizas-api
    ports:
      - "8080:8080"
    environment:
      APP_API_KEY: "123456"
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:8080/actuator/health"]
      interval: 10s
      timeout: 5s
      retries: 5
